import 'package:flutter/material.dart';

class DirectiveList extends StatelessWidget {
  const DirectiveList({super.key});

  @override
  Widget build(BuildContext context) {
    return Expanded(child: Text('data'));
  }
}
