import 'package:flutter/material.dart';

class OrganizeFileMenu extends StatelessWidget {
  const OrganizeFileMenu({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('整理文件'),
    );
  }
}
